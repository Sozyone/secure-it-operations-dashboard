---
- name: Prepare local DevSecOps lab server
  hosts: local
  become: true

  tasks:
    - name: Install basic packages
      apt:
        name:
          - git
          - curl
          - nano
          - python3
          - python3-venv
          - python3-pip
        state: present
        update_cache: true
